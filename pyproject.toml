


[project]
name = "mdsp"
version = "0.1.0"
description = "Python audio DSP library with kernels written in Mojo"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
keywords = ["audio", "dsp", "signal-processing", "mojo", "synthesis", "filters"]
dependencies = [
    "mojo>=1.0.0,<1.1",
    "numpy>=2.2.6",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Programming Language :: Python :: Implementation :: CPython",
    "Topic :: Multimedia :: Sound/Audio",
    "Typing :: Typed",
]

[dependency-groups]
dev = [
    "mypy>=2.3.1",
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
    "ruff>=0.16.7",
    "twine>=7.0.0",
]

[build-system]
requires = ["uv_build>=0.12.15,<0.13"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "mdsp"
module-root = "src"
source-include = ["tests/**", "CHANGELOG.md", "LICENSE"]


[tool.pytest.ini_options]
testpaths = ["tests", "src/mdsp"]
addopts = "--doctest-modules"

[tool.ruff]
src = ["src", "tests"]

[tool.mypy]
strict = true
